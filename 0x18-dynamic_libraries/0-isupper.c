#include "main.h"
/**
 * _isupper - Check if a letter is upper case
 * @c:	Number to be checked
 * Return: 1 for upper case else 0
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
