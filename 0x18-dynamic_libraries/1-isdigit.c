#include "main.h"
/**
 * _isdigit - Check if character is a digit
 * @c:	Number to be checked
 * Return: 1 for character else 0
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
